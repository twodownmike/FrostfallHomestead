# Overworld Map Detail Sheet

Source: user-provided image from `/home/mikecross/Downloads/Gemini_Generated_Image_talvdetalvdetalv.png`.

The sheet was copied to `source_sheet.png` and cropped into individual map-detail PNGs for snowy terrain, roads, resource nodes, and points of interest.

The provided image has an opaque checkerboard background, so the crops use background keying to remove the checker pattern as much as possible.
